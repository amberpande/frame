"""Frame — dashboards as data.

One React runtime renders any dashboard from a versioned spec. This package is
the other half: the semantic model the specs reference, the compiler that turns
a spec's query into SQL, and the serving tiers that keep most interactions away
from the warehouse.
"""

__version__ = "0.1.0"
